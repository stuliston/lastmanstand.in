<h2>Current Games</h2>
<ul class="games-list unstyled">
  {{#each game in controller}}
    <li>
      {{#linkTo 'game' game}}
        <div class="name">{{game.name}}</div>
      {{/linkTo}}
    </li>
  {{/each}}
</ul>


<h2>Invitations</h2>