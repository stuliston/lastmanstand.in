LMS.GameMembersRoute = Ember.Route.extend()

  # model: (params)->
  #   game = @modelFor('game')
  #   game.get('gameMemberships')

  # setupController: (controller, gameMemberships) ->
  #   controller.set('model', gameMemberships)