LMS.GameCurrentRoundRoute = Ember.Route.extend()

  # model: (params)->
  #   game = @modelFor('game')
  #   game.get('currentSeason.rounds.firstObject') #Make this calculate properly later

  # setupController: (controller, round) ->
  #   controller.set('model', round)