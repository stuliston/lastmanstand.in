<%- content_for :head do %>

  <script>
    var currentProfileId = <%= current_user.profile.id %>;
    window.ENV = { EXPERIMENTAL_CONTROL_HELPER: true};

    var profileData = <%= ProfileSerializer.new(current_user.profile, scope: current_user).to_json.html_safe %>;
  </script>

  <%= javascript_include_tag "vendor" %>
  <%= javascript_include_tag "lastmanstanding" %>

<%- end %>

<div id="viewporter">
</div>