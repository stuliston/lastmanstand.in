development:
  adapter: postgresql
  encoding: utf8
  database: lastmanstanding_dev
  pool: 5
  host: localhost

test:
  adapter: postgresql
  encoding: utf8
  database: lastmanstanding_test
  pool: 5
  host: localhost

# production:
#   adapter: postgresql
#   encoding: utf8
#   database: lastmanstanding_dev
#   pool: 5
#   host: localhost
